# 舒时雨公开履历网站

此目录是独立 GitHub Pages 仓库。它只包含明确允许公开的页面、简历下载文件、精选图片和公开数据。

本地预览：在此目录运行 `py -3 -m http.server 8000`，再打开 `http://localhost:8000`。

首次发布：创建 GitHub 公开仓库后，将其设为 `origin`，推送 `main` 分支，并在仓库设置中将 Pages Source 设为 **GitHub Actions**。
